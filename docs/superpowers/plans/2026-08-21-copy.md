# UPCopy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现与 uview-plus 3.8.86 `u-copy` 接口尽量一致、可测试并使用 Apple 原生剪贴板的 SwiftUI `UPCopy` 组件。

**Architecture:** `UPCopy<Content>` 使用原生 SwiftUI `Button` 包装默认或自定义 slot，并将剪贴板写入抽象为内部 `(String) -> Bool` 闭包。生产实现按平台映射 `UIPasteboard` 或 `NSPasteboard`，测试注入假写入器验证空内容、成功和失败路径；`alertStyle` 与 `notice` 保留为兼容元数据，不耦合 Toast、Modal 或 Fastview。

**Tech Stack:** Swift 6、SwiftUI、UIKit `UIPasteboard`、AppKit `NSPasteboard`、XCTest、Swift Package Manager、Xcode iOS Simulator 构建。

## Global Constraints

- 对应上游版本固定为 uview-plus `3.8.86`。
- 公开兼容 props 固定为 `content: String = ""`、`alertStyle: String = "toast"`、`notice: String = "复制成功"`。
- default slot 映射为 `Text("复制")`，自定义 slot 映射为 `@ViewBuilder`。
- `success` 映射为 `.onSuccess { ... }`，仅原生剪贴板写入成功后调用一次。
- 空 `content` 不访问剪贴板且不触发成功事件；写入失败也不触发成功事件。
- 支持 Package 声明的 iOS 17+ 和 macOS 14+，系统剪贴板实现必须使用条件编译。
- `alertStyle` 与 `notice` 本轮只保留元数据，不依赖 `UPToast`、`UPModal` 或全局反馈环境。
- 不引入 Fastview 专用 API。
- 只使用当前主工作区，不创建、切换或使用 git worktree，也不派发会产生隔离工作区的协作者。
- 不执行 `git reset`、`git clean` 或 `git add .`。
- 不修改 `UltraUI/Sources/UltraUI/Core/UPConfig.swift`，不覆盖或提交任何既有 dirty / untracked 文件。
- 每次只用完整路径精确暂存本任务目标文件。

## File Structure

- Create: `UltraUI/Sources/UltraUI/Components/UPCopy.swift` — `UPCopy` 公开视图、链式成功事件、可注入写入器和 Apple 平台剪贴板适配。
- Create: `UltraUI/Tests/UltraUITests/CopyTests.swift` — props、slot 构造、空值、成功、失败和链式事件测试。
- Modify: `UVIEW_PLUS_PROGRESS.md` — 将第 34 行 `u-copy` 标记为完成，并更新基线、计数和覆盖率。

---

### Task 1: 用 TDD 实现 UPCopy 接口与原生剪贴板行为

**Files:**
- Create: `UltraUI/Tests/UltraUITests/CopyTests.swift`
- Create: `UltraUI/Sources/UltraUI/Components/UPCopy.swift`

**Interfaces:**
- Consumes: SwiftUI `View` / `Button` / `Text`，iOS `UIPasteboard.general`，macOS `NSPasteboard.general`。
- Produces: `public struct UPCopy<Content: View>: View`。
- Produces: `public init(content: String = "", alertStyle: String = "toast", notice: String = "复制成功") where Content == Text`。
- Produces: `public init(content: String = "", alertStyle: String = "toast", notice: String = "复制成功", @ViewBuilder contentView: () -> Content)`。
- Produces: `public func onSuccess(_ action: @escaping () -> Void) -> UPCopy`。
- Produces for internal testing: initializer with `clipboardWriter: @escaping (String) -> Bool` and `@discardableResult func performCopy() -> Bool`。

- [ ] **Step 1: 写覆盖完整行为的失败测试**

创建 `UltraUI/Tests/UltraUITests/CopyTests.swift`：

```swift
import SwiftUI
import XCTest
@testable import UltraUI

@MainActor
final class CopyTests: XCTestCase {
    func testDefaultsMatchUviewPlusCopy() {
        let copy = UPCopy()

        XCTAssertEqual(copy.content, "")
        XCTAssertEqual(copy.alertStyle, "toast")
        XCTAssertEqual(copy.notice, "复制成功")
    }

    func testPropsAndCustomSlotCanBeConstructed() {
        let copy = UPCopy(
            content: "hello",
            alertStyle: "modal",
            notice: "已复制",
            clipboardWriter: { _ in true }
        ) {
            Text("复制链接")
        }

        XCTAssertEqual(copy.content, "hello")
        XCTAssertEqual(copy.alertStyle, "modal")
        XCTAssertEqual(copy.notice, "已复制")
    }

    func testEmptyContentDoesNotWriteOrEmitSuccess() {
        var writtenValues: [String] = []
        var successCount = 0
        let copy = UPCopy(
            clipboardWriter: {
                writtenValues.append($0)
                return true
            }
        )
        .onSuccess { successCount += 1 }

        XCTAssertFalse(copy.performCopy())
        XCTAssertTrue(writtenValues.isEmpty)
        XCTAssertEqual(successCount, 0)
    }

    func testSuccessfulCopyWritesExactContentAndEmitsSuccessOnce() {
        var writtenValues: [String] = []
        var successCount = 0
        let copy = UPCopy(
            content: "https://example.com",
            clipboardWriter: {
                writtenValues.append($0)
                return true
            }
        )
        .onSuccess { successCount += 1 }

        XCTAssertTrue(copy.performCopy())
        XCTAssertEqual(writtenValues, ["https://example.com"])
        XCTAssertEqual(successCount, 1)
    }

    func testFailedClipboardWriteDoesNotEmitSuccess() {
        var successCount = 0
        let copy = UPCopy(
            content: "hello",
            clipboardWriter: { _ in false }
        )
        .onSuccess { successCount += 1 }

        XCTAssertFalse(copy.performCopy())
        XCTAssertEqual(successCount, 0)
    }

    func testOnSuccessPreservesCompatibilityProps() {
        let copy = UPCopy(
            content: "hello",
            alertStyle: "modal",
            notice: "done",
            clipboardWriter: { _ in true }
        )
        .onSuccess {}

        XCTAssertEqual(copy.content, "hello")
        XCTAssertEqual(copy.alertStyle, "modal")
        XCTAssertEqual(copy.notice, "done")
    }
}
```

- [ ] **Step 2: 运行定向测试并确认 RED 原因正确**

运行：

```bash
cd UltraUI
swift test --filter CopyTests
```

预期：编译失败，核心错误为 `cannot find 'UPCopy' in scope`；不得因为测试语法错误或既有 dirty 文件损坏而进入实现。

- [ ] **Step 3: 写最小生产实现使全部 CopyTests 通过**

创建 `UltraUI/Sources/UltraUI/Components/UPCopy.swift`：

```swift
import SwiftUI

#if canImport(UIKit)
import UIKit
#elseif canImport(AppKit)
import AppKit
#endif

typealias UPClipboardWriter = (String) -> Bool

@MainActor
enum UPSystemClipboard {
    static func write(_ content: String) -> Bool {
        #if canImport(UIKit)
        UIPasteboard.general.string = content
        return true
        #elseif canImport(AppKit)
        let pasteboard = NSPasteboard.general
        pasteboard.clearContents()
        return pasteboard.setString(content, forType: .string)
        #else
        return false
        #endif
    }
}

/// Native SwiftUI counterpart of uview-plus `u-copy`.
@MainActor
public struct UPCopy<Content: View>: View {
    var content: String
    var alertStyle: String
    var notice: String
    var contentView: Content
    var successAction: (() -> Void)?
    var clipboardWriter: UPClipboardWriter

    public init(
        content: String = "",
        alertStyle: String = "toast",
        notice: String = "复制成功"
    ) where Content == Text {
        self.init(
            content: content,
            alertStyle: alertStyle,
            notice: notice,
            clipboardWriter: UPSystemClipboard.write,
            contentView: { Text("复制") }
        )
    }

    public init(
        content: String = "",
        alertStyle: String = "toast",
        notice: String = "复制成功",
        @ViewBuilder contentView: () -> Content
    ) {
        self.init(
            content: content,
            alertStyle: alertStyle,
            notice: notice,
            clipboardWriter: UPSystemClipboard.write,
            contentView: contentView
        )
    }

    init(
        content: String = "",
        alertStyle: String = "toast",
        notice: String = "复制成功",
        clipboardWriter: @escaping UPClipboardWriter
    ) where Content == Text {
        self.init(
            content: content,
            alertStyle: alertStyle,
            notice: notice,
            clipboardWriter: clipboardWriter,
            contentView: { Text("复制") }
        )
    }

    init(
        content: String = "",
        alertStyle: String = "toast",
        notice: String = "复制成功",
        clipboardWriter: @escaping UPClipboardWriter,
        @ViewBuilder contentView: () -> Content
    ) {
        self.content = content
        self.alertStyle = alertStyle
        self.notice = notice
        self.contentView = contentView()
        self.successAction = nil
        self.clipboardWriter = clipboardWriter
    }

    public var body: some View {
        Button(action: { performCopy() }) {
            contentView
        }
        .buttonStyle(.plain)
    }

    @discardableResult
    func performCopy() -> Bool {
        guard !content.isEmpty, clipboardWriter(content) else { return false }
        successAction?()
        return true
    }

    public func onSuccess(_ action: @escaping () -> Void) -> UPCopy {
        var copy = self
        copy.successAction = action
        return copy
    }
}
```

如果 Swift 6 对 `Button(action:)` 中忽略 Bool 返回值发出类型错误，将 action 明确写为：

```swift
Button(action: { _ = performCopy() }) {
    contentView
}
```

这是唯一允许的等价语法调整，不改变接口或行为。

- [ ] **Step 4: 运行定向测试并确认 GREEN**

运行：

```bash
cd UltraUI
swift test --filter CopyTests
```

预期：`CopyTests` 中 6 个测试全部通过，0 failures。

- [ ] **Step 5: 检查组件和测试差异范围**

运行：

```bash
git diff --check -- \
  UltraUI/Sources/UltraUI/Components/UPCopy.swift \
  UltraUI/Tests/UltraUITests/CopyTests.swift
git status --short -- \
  UltraUI/Sources/UltraUI/Components/UPCopy.swift \
  UltraUI/Tests/UltraUITests/CopyTests.swift
```

预期：仅显示两个新文件，无 whitespace error。

- [ ] **Step 6: 精确暂存、核验并提交组件**

运行：

```bash
git add -- \
  UltraUI/Sources/UltraUI/Components/UPCopy.swift \
  UltraUI/Tests/UltraUITests/CopyTests.swift
git diff --cached --check
git diff --cached --name-only
git commit -m "feat: add copy component"
```

预期暂存路径恰好为上述两个文件，不含任何既有 dirty 文件。

---

### Task 2: 全量验证并更新 uview-plus 进度文档

**Files:**
- Modify: `UVIEW_PLUS_PROGRESS.md`

**Interfaces:**
- Consumes: Task 1 已提交的 `UPCopy` 和 `CopyTests`。
- Produces: 进度表中 `u-copy` 的稳定完成记录和最新已提交基线。

- [ ] **Step 1: 运行新鲜的 Swift 全量测试**

运行：

```bash
cd UltraUI
swift test
```

预期：全部测试通过，0 failures；结果包含 `CopyTests` 的 6 个测试。

- [ ] **Step 2: 运行 Demo iOS Simulator 构建**

运行：

```bash
cd ..
xcodebuild \
  -project UltraUIDemo.xcodeproj \
  -scheme UltraUIDemo \
  -destination 'generic/platform=iOS Simulator' \
  build \
  CODE_SIGNING_ALLOWED=NO
```

预期：命令退出码为 0，并输出 `** BUILD SUCCEEDED **`。

- [ ] **Step 3: 更新进度表的基线、计数和 u-copy 行**

先运行：

```bash
git rev-parse --short HEAD
```

将输出的 Task 1 组件提交哈希写入 `UVIEW_PLUS_PROGRESS.md` 顶部 `iOS 基线`。同时作以下精确更新：

```text
已完成：44 → 45
兼容增强中：11（不变）
开发中：0（不变）
待开始：83 → 82
已有实现覆盖：55 / 138 → 56 / 138，约 40.6%
已提交覆盖：55 / 138 → 56 / 138，约 40.6%
```

将第 34 行替换为：

```markdown
| 34 | `u-copy` | `UPCopy` | ✅ 已完成 | 高 | 原生剪贴板、props、默认/自定义 slot、success 及空内容/失败行为已覆盖 |
```

- [ ] **Step 4: 校验进度文档内容和统计**

运行：

```bash
rg -n 'iOS 基线|已完成|待开始|已有实现覆盖|已提交覆盖|`u-copy`' UVIEW_PLUS_PROGRESS.md
git diff --check -- UVIEW_PLUS_PROGRESS.md
git diff -- UVIEW_PLUS_PROGRESS.md
```

预期：只出现 Task 2 指定的基线、5 个统计字段和 `u-copy` 行变更，统计总和 `45 + 11 + 0 + 82 = 138`。

- [ ] **Step 5: 精确暂存、核验并提交进度文档**

运行：

```bash
git add -- UVIEW_PLUS_PROGRESS.md
git diff --cached --check
git diff --cached --name-only
git commit -m "docs: complete copy component tracking"
```

预期暂存路径恰好为 `UVIEW_PLUS_PROGRESS.md`。

- [ ] **Step 6: 提交后做最终状态审计**

运行：

```bash
git log --oneline -4
git status --short
git diff --cached --name-only
```

预期：最近提交包含设计规格、实施计划、组件和进度记录；暂存区为空。工作区仍可显示用户既有 dirty / untracked 文件，但不得新增 UPCopy 或进度文档的未提交修改，也不得声称整体工作区干净。
