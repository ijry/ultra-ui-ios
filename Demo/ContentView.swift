import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            List {
                Text("UltraUI Demo")
            }
            .navigationTitle("UltraUI")
        }
    }
}
